<Window x:Class="HybridApp.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Hybrid High-Performance App" Height="800" Width="1200"
        Background="#0b0e11">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#181a20" Padding="15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="BITCOIN DASHBOARD" FontSize="18" FontWeight="Bold" Foreground="#f3ba2f" VerticalAlignment="Center" Margin="0,0,30,0"/>
                
                <TextBlock Text="Data Points:" Foreground="#848e9c" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Text="{Binding DataPointCount, UpdateSourceTrigger=PropertyChanged}" 
                         Background="#2b2f36" Foreground="White" BorderThickness="0" Padding="8,4"
                         Width="120" VerticalContentAlignment="Center" Margin="0,0,15,0"/>
                
                <Button Content="Generate Data" 
                        Command="{Binding GenerateDataCommand}" 
                        Background="#f3ba2f" Foreground="#1e2329" BorderThickness="0"
                        Padding="20,8" Margin="0,0,25,0" FontWeight="Bold" Cursor="Hand">
                </Button>
                
                <TextBlock Text="{Binding StatusMessage}" FontSize="14" Foreground="#848e9c" VerticalAlignment="Center" FontStyle="Italic"/>
            </StackPanel>
        </Border>

        <wv2:WebView2 Name="webView" Grid.Row="1"/>
    </Grid>
</Window>
